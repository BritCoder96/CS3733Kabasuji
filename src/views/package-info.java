/**
 * views
 * 
 * Provides a GUI for the game. 
 * When interacted with, calls classes in the controllers package to handle the input, and is then updated by classes in the controllers package.
 * Works as the Boundary portion of an EBC model.
 * 
*/

package views;